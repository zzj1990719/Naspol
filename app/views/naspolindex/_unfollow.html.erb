<div id="<%= @web.id %>">
<%= form_for(@web, html: { method: :delete },remote: true) do |f| %>
	  <%= f.label :id%>
	  <%= f.hidden_field :id %>
      <%= f.submit "Unfollow", class: "btn btn-large btn-primary" %>
      <% end %> 
</div>	  